{
  "name": "ai4s-workbench",
  "version": "0.3.0",
  "private": true,
  "description": "Open Science Desktop — local-first, model-agnostic AI research workbench for macOS, Windows and Linux.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @ai4s/desktop dev",
    "build": "pnpm --filter @ai4s/desktop build",
    "test": "pnpm --filter @ai4s/desktop test",
    "typecheck": "pnpm --filter @ai4s/desktop typecheck",
    "lint": "pnpm --filter @ai4s/desktop lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.4.0",
  "//overrides": {
    "brace-expansion@1": "Dependabot #4 (high): brace-expansion < 1.1.16 expands consecutive empty {} groups in exponential time. It reaches a PRODUCTION install through exceljs → archiver → glob@7 → minimatch@3, none of which we can bump directly. Scoped to the 1.x line so the 2.x and 5.x copies other packages depend on are left alone. Drop this once exceljs ships a modern archiver."
  },
  "pnpm": {
    "overrides": {
      "brace-expansion@1": "^1.1.16"
    }
  }
}
